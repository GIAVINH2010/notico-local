<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> </title>
  <style>
    .header {
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      flex: 1;
    }

    .h1 {
      flex: 1;
    }

    .timeslots-table {
      /* background-color: blue; */
      border: 1px solid darkgrey;
      border-bottom: 0px;
      margin-bottom: 20px;
      min-width: 300px;
    }

    .date {
      padding: 5px 10px 5px 10px;
      background-color: darksalmon;
      border-bottom: 1px solid darkgrey;
    }

    .slot {
      padding: 5px 10px 5px 10px;
      border-bottom: 1px solid darkgrey;
    }

    .content {
      display: flex;
      flex-direction: column;
      align-items: center
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="header">
      <div class="logo">
        <a href="/"><img src="public/images/logo.png" alt="logo"></a>
      </div>
      <div class="h1">
        <h1>Order N° <%= cmdId %> from <%= customerName %> </h1>
      </div>
    </div>

    <div class="content" id="pickerSection">
      <h2>Pick a delivery slot</h2>

      <p>To pick a timeslot, click one the corresponding line. If no slot suits to you, please contact us: 0140414243
      </p>
      <% if(dates.length !== 0) { %>
      <form class="timeslots-table" id="timeslots">

        <% 
              var index = 1
              for(var i=0; i<dates.length; i++) { 
              
            %>

        <div class="option<%= index %>">
          <div class="date" data-id="<%=index%>"><%= dates[i] %> </div>
          <% for(var k=0; k<slots.length; k++) { %>
          <div class="slot">
            <input type="radio" name="timeslots" data-id="<%=index%>"
              value="<%= slots[k].startTime%> - <%= slots[k].endTime %>">
            <label><%= slots[k].startTime%> - <%= slots[k].endTime %></label>
          </div>
          <% 
                } 
              %>
        </div>
        <% index++ } %>
      </form>

      <button id="continue">Continue</button>
      <% } %>
    </div>
    <div class="content" id="confirmation"></div>
    <div class="content" id="lastStep"></div>

    <script src="https://code.jquery.com/jquery-3.4.1.min.js"
      integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous"></script>
    <script src="public/javascripts/apt.js"></script>


</body>

</html>